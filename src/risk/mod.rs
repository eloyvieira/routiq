/*
pub fn validate_amount(amount: f64, max_amount: f64) -> anyhow::Result<()> {
    if amount <= 0.0 {
        anyhow::bail!("amount must be positive");
    }
    if amount > max_amount {
        anyhow::bail!("amount exceeds configured limit");
    }
    Ok(())
}
*/